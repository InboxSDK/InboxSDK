/**
* @class
* Object that represents the global view.
*/
var Global = /** @lends Global */ {

	/**
  * Inserts a content panel into the sidebar of the global view. A content panel
  * simply displays your content to the user, typically in the form of a
  * sidebar. The global view can have multiple content panels added to them and
  * the SDK will handle inserting their icons them into the global view.
	* ^gmail
	* @param  {ContentPanelDescriptor} contentPanelDescriptor - The details of the content panel to add to the global sidebar.
	* @return {Promise.<ContentPanelView>}
	*/
	addGlobalSidebarContentPanel: function(){},
};
