/* @flow */

import Kefir from 'kefir';

import type { Options } from 'event-listener-with-options';

declare export default function fromEventsWithOptions(
  target: EventTarget,
  eventName: string,
  options: Options | boolean
): Kefir.Observable<any>;
