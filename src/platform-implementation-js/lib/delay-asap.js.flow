/* @flow */

import Kefir from 'kefir';

// Returns a stream that emits a value in the next event loop run. Works well
// with flatmap.
declare export default function delayAsap<T>(value: T): Kefir.Observable<T>;
