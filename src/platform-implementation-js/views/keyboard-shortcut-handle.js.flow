/* @flow */

import EventEmitter from '../lib/safe-event-emitter';

declare export default class KeyboardShortcutHandle extends EventEmitter {
  chord: string;
  description: string;
  constructor(chord: string, description: string): KeyboardShortcutHandle;
  remove(): void;
}
